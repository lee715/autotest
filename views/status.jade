html
	head
		meta(http-equiv="Content-Type",content="text/html; charset=utf-8")
		title Status
	body
		span 请求已提交。
		a(href="/result") 点此查看结果
		span ，
		a(href="/index") 点此继续添加测试
		p#status
	script(src="../js/jquery.js")
	script.
		setInterval(function(){
			$.ajax({
				url: '/getQueueInfo'
			})
			.done(function(res){
				var str = '队列中尚有个'+res.len+'个任务，预计等待时间:'+res.time+' h';
				$('#status').html(str);
			});
		}, 10000);
		$.ajax({
			url: '/getQueueInfo'
		})
		.done(function(res){
			var str = '队列中尚有个'+res.len+'个任务，预计等待时间:'+res.time+' h';
			$('#status').html(str);
		});

